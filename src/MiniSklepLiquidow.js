import { useState, useEffect } from "react"; import bg from "./assets/bg-liquid.png"; const SHEET_API = "https://script.google.com/macros/s/AKfycbzzidf4TZnkJ59YeubZQknj_Y3w0blwxNCpXa1LiSe2oEfXYo8CUMnTJXKHUZFuezFR/exec"; export default function MiniSklepLiquidow() { const [serverInventory, setServerInventory] = useState({}); const [selectedFlavor, setSelectedFlavor] = useState(null); const [name, setName] = useState( () => localStorage.getItem("miniSklepName") || "" ); const [ml, setMl] = useState( () => localStorage.getItem("miniSklepMl") || "" ); const [strength, setStrength] = useState(() => { const s = localStorage.getItem("miniSklepStrength"); return s ? Number(s) : null; }); const [base, setBase] = useState( () => localStorage.getItem("miniSklepBase") || null ); const [cart, setCart] = useState(() => { const c = localStorage.getItem("miniSklepCart"); return c ? JSON.parse(c) : []; }); const [message, setMessage] = useState(""); const [messageType, setMessageType] = useState("info"); const [isSending, setIsSending] = useState(false); // ================= HELPERS ================= const showMessage = (txt, type = "info") => { setMessage(txt); setMessageType(type); setTimeout(() => { setMessage(""); }, 4000); }; // ================= FETCH INVENTORY ================= useEffect(() => { const fetchInventory = () => { fetch(SHEET_API) .then((r) => r.json()) .then((d) => setServerInventory(d)) .catch(console.error); }; fetchInventory(); const interval = setInterval(fetchInventory, 5000); return () => clearInterval(interval); }, []); // ================= SAVE ================= useEffect(() => { localStorage.setItem("miniSklepName", name); }, [name]); useEffect(() => { localStorage.setItem("miniSklepMl", ml); }, [ml]); useEffect(() => { localStorage.setItem("miniSklepStrength", strength ?? ""); }, [strength]); useEffect(() => { localStorage.setItem("miniSklepBase", base ?? ""); }, [base]); useEffect(() => { localStorage.setItem("miniSklepCart", JSON.stringify(cart)); }, [cart]); // ================= VALIDATION ================= useEffect(() => { if (strength === 36 && base === "nikotyna") { setBase(null); } }, [strength, base]); useEffect(() => { if (base === "nikotyna" && strength === 36) { setStrength(null); } }, [base, strength]); // ================= STOCK ================= const getReservedInCart = (flavorId) => cart .filter((i) => i.flavor.id === flavorId) .reduce((s, i) => s + i.ml / 10, 0); const getAvailableMl = (flavorId) => { const server = serverInventory[flavorId] || 0; const reserved = getReservedInCart(flavorId); return Math.max(0, (server - reserved) * 10); }; // ================= PRICE ================= const calculatePrice = (volume, strength, baseType) => { let price = 0; let p10 = 0, p60 = 0; if (baseType === "sól") { if ([6, 12, 18].includes(strength)) { p10 = 15; p60 = 79; } else { p10 = 16; p60 = 85; } } else { if ([6, 12].includes(strength)) { p10 = 11; p60 = 55; } else if (strength === 18) { p10 = 12; p60 = 61; } else if (strength === 24) { p10 = 13; p60 = 67; } } let remainder = volume; const num60 = Math.floor(remainder / 60); price += num60 * p60; remainder %= 60; const num30 = Math.floor(remainder / 30); if (num30 > 0) { const price30 = (() => { if (baseType === "nikotyna") { if ([6, 12].includes(strength)) return 32.5; if (strength === 18) return 35.5; if (strength === 24) return 38.5; } else { if ([6, 12, 18].includes(strength)) return 44.5; if ([24, 36].includes(strength)) return 47.5; } return 0; })(); price += num30 * price30; remainder %= 30; } price += (remainder / 10) * p10; return price; }; // ================= ADD TO CART ================= const addToCart = () => { if (!selectedFlavor) return showMessage("❌ Wybierz smak", "error"); if (!ml) return showMessage("❌ Podaj ilość", "error"); if (ml % 10 !== 0) return showMessage("❌ Tylko co 10ml", "error"); if (!strength) return showMessage("❌ Wybierz moc", "error"); if (!base) return showMessage("❌ Wybierz bazę", "error"); const maxMl = getAvailableMl(selectedFlavor.id); if (ml > maxMl) return showMessage(❌ Max ${maxMl}ml, "error"); const price = calculatePrice( Number(ml), strength, base ); setCart([ ...cart, { flavor: selectedFlavor, ml: Number(ml), strength, base, price, }, ]); setMl(""); showMessage("✅ Dodano do koszyka", "success"); }; const removeItem = (idx) => setCart(cart.filter((_, i) => i !== idx)); // ================= SEND ================= const sendOrder = async () => { if (!name) return showMessage("❌ Podaj imię", "error"); if (cart.length === 0) return showMessage("❌ Koszyk pusty", "error"); if (isSending) return; setIsSending(true); const orderText = cart .map( (i) => ${i.flavor.id}/${i.ml}ml/${i.strength}mg/${i.base}/${i.price.toFixed(2)} ) .join("\n"); const total = cart.reduce( (s, i) => s + i.price, 0 ); const usedAromas = {}; cart.forEach((i) => { usedAromas[i.flavor.id] = (usedAromas[i.flavor.id] || 0) + i.ml / 10; }); try { await fetch(SHEET_API, { method: "POST", body: JSON.stringify({ name, orderText, total, usedAromas, }), }); showMessage( "✅ Zamówienie wysłane! Odezwij się po odbiór 😎", "success" ); localStorage.clear(); setCart([]); setName(""); setMl(""); setStrength(null); setBase(null); setSelectedFlavor(null); } catch { showMessage("❌ Błąd wysyłki", "error"); } finally { setIsSending(false); } }; const total = cart.reduce( (s, i) => s + i.price, 0 ); // ================= CATEGORY ================= const categoryColors = { // TWOJE KATEGORIE }; const flavorCategories = { // TWOJE SMAKI }; return ( <div style={{ maxWidth: 520, margin: "40px auto", padding: 15, borderRadius: 12, background: url(${bg}) center/cover, boxShadow: "0 0 20px rgba(0,0,0,.2)", }} > <h2 style={{ textAlign: "center" }}> Mini sklep liquidów </h2> <input placeholder="Imię i Nazwisko" value={name} onChange={(e) => setName(e.target.value)} style={{ width: "50%", padding: "4px 6px", marginBottom: 10, fontSize: 18, }} /> <h3>Smaki</h3> {Object.entries(flavorCategories).map( ([cat, flavors]) => { const [main, light] = categoryColors[cat]; return ( <details key={cat} style={{ marginBottom: 10, borderRadius: 8, padding: 5, background: main, }} > <summary style={{ fontWeight: "bold", padding: 6, }} > {cat} </summary> <div style={{ padding: 6, display: "flex", flexDirection: "column", gap: 4, }} > {flavors.map((f) => { const stock = getAvailableMl(f.id); const stockColor = stock === 0 ? "red" : stock < 120 ? "#facc15" : "#22c55e"; return ( <label key={f.id} style={{ display: "flex", alignItems: "center", fontSize: 13, background: linear-gradient(90deg, ${light}, #fff), borderRadius: 6, padding: "4px 6px", cursor: stock === 0 ? "not-allowed" : "pointer", opacity: stock === 0 ? 0.6 : 1, transition: ".2s", }} onClick={() => { if (stock === 0) { showMessage( "❌ Brak na stanie", "error" ); } else { setSelectedFlavor(f); } }} > <span style={{ width: 16, height: 16, border: "1px solid #000", display: "inline-block", marginRight: 6, textAlign: "center", lineHeight: "16px", background: selectedFlavor?.id === f.id ? "green" : "#fff", color: "#fff", }} > {selectedFlavor?.id === f.id ? "✔" : ""} </span> <div style={{ flex: 1 }}> {f.id}. {f.name} {[1, 14, 27, 36].includes( f.id ) && ( <span className="bestseller"> 🔥 BESTSELLER </span> )} <div style={{ marginTop: 2, fontWeight: "bold", color: stockColor, }} > na stanie: {stock}ml </div> {stock <= 60 && stock > 0 && ( <div className="lowStock"> ⚠️ Końcówka smaku </div> )} </div> </label> ); })} </div> </details> ); } )} <h3>Baza</h3> {["Nikotyna", "Sól"].map((v) => { const disabled = v === "Nikotyna" && strength === 36; return ( <div key={v} onClick={() => !disabled && setBase(v.toLowerCase()) } style={{ display: "inline-block", width: 70, height: 30, marginRight: 6, border: "1px solid #000", borderRadius: 4, textAlign: "center", lineHeight: "30px", cursor: disabled ? "not-allowed" : "pointer", background: base?.toLowerCase() === v.toLowerCase() ? "green" : "#eee", color: base?.toLowerCase() === v.toLowerCase() ? "#fff" : "#000", }} > {v} </div> ); })} <h3>Moc</h3> {[6, 12, 18, 24, 36].map((v) => { const disabled = base === "nikotyna" && v === 36; return ( <div key={v} onClick={() => !disabled && setStrength(v) } style={{ display: "inline-block", width: 40, height: 30, marginRight: 6, border: "1px solid #000", borderRadius: 4, textAlign: "center", lineHeight: "30px", cursor: disabled ? "not-allowed" : "pointer", background: strength === v ? "green" : "#eee", color: strength === v ? "#fff" : "#000", }} > {v}mg </div> ); })} <h3>Ilość (ml)</h3> <div style={{ display: "flex", alignItems: "center", gap: 12, }} > <input type="number" step={10} min={10} value={ml} onChange={(e) => setMl(e.target.value) } style={{ width: "30%", padding: "4px 6px", fontSize: 18, }} /> <span style={{ fontSize: 12, fontWeight: 700, color: "#b91c1c", background: "#fef08a", padding: "2px 6px", borderRadius: 6, animation: "pulse 1s infinite", }} > 🌟 Kupując 60ml oszczędzasz! </span> </div> {ml > 0 && ml < 60 && ( <div className="progressBox"> 🔥 Do najlepszej ceny brakuje Ci tylko{" "} {60 - Number(ml)}ml </div> )} <button onClick={addToCart} className={addBtn ${ Number(ml) === 60 ? "bestPrice" : "" }} style={{ width: "100%", marginTop: 10, padding: 12, borderRadius: 8, background: "#22c55e", color: "#fff", border: "none", fontSize: 16, }} > {Number(ml) === 60 ? "🔥 Najlepsza opcja — dodaj 60ml" : "➕ Dodaj do koszyka"} </button> {message && ( <div className={ messageType === "success" ? "successPulse" : "" } style={{ marginTop: 8, padding: 8, background: messageType === "error" ? "#fecaca" : "#bbf7d0", borderRadius: 6, textAlign: "center", fontWeight: "bold", }} > {message} </div> )} <h3>Koszyk</h3> {cart.map((i, idx) => ( <div key={idx} style={{ marginBottom: 4 }} > {i.flavor.id}/{i.ml}ml/ {i.strength}mg/{i.base} —{" "} {i.price.toFixed(2)}zł <button onClick={() => removeItem(idx)} style={{ marginLeft: 6 }} > ❌ </button> </div> ))} <h3 style={{ marginTop: 14, textAlign: "center", fontSize: 24, }} > 💰 Suma: {total.toFixed(2)} zł </h3> <div className="cartFloating"> 🛒 Koszyk: {cart.length} produktów —{" "} {total.toFixed(2)} zł </div> <button disabled={isSending} onClick={sendOrder} className="addBtn" style={{ width: "100%", marginTop: 15, padding: 12, background: isSending ? "#9ca3af" : "#16a34a", color: "#fff", border: "none", borderRadius: 8, fontSize: 18, fontWeight: "bold", }} > {isSending ? "⏳ Wysyłanie..." : "📤 Wyślij zamówienie"} </button> <style>{ @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.05); } 100% { transform: scale(1); } } @keyframes glowGreen { 0% { box-shadow: 0 0 0px #22c55e; } 50% { box-shadow: 0 0 20px #22c55e; } 100% { box-shadow: 0 0 0px #22c55e; } } .addBtn { transition: all .2s ease; font-weight: bold; } .addBtn:hover { transform: scale(1.03); } .addBtn:active { transform: scale(.96); } .bestPrice { animation: glowGreen 1.5s infinite; } .lowStock { animation: pulse 1s infinite; color: #dc2626; font-weight: bold; font-size: 12px; } .bestseller { background: #ef4444; color: white; padding: 2px 6px; border-radius: 6px; font-size: 10px; margin-left: 6px; font-weight: bold; } .progressBox { margin-top: 8px; background: #fef08a; color: #92400e; padding: 8px; border-radius: 8px; font-size: 13px; font-weight: bold; text-align: center; animation: pulse 1.2s infinite; } .cartFloating { position: sticky; bottom: 10px; background: #111; color: #fff; padding: 10px 14px; border-radius: 12px; text-align: center; font-weight: bold; margin-top: 10px; box-shadow: 0 0 20px rgba(0,0,0,.3); z-index: 999; } .successPulse { animation: pulse 1s infinite; } }</style> </div> ); }
